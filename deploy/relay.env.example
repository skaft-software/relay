# Copy to /etc/relay/relay.env and adjust values.
# Ensure this file is readable only by the relay user (chmod 600).
HOST=127.0.0.1
PORT=1234
UPSTREAM_BASE_URL=http://127.0.0.1:8080/v1
DEFAULT_MODEL=
REQUEST_TIMEOUT_SECONDS=600
MAX_REQUEST_BODY_BYTES=1048576
LOG_LEVEL=info
API_KEY=change-me-in-production

# Security hardening
CORS_ORIGIN=
RATE_LIMIT_AUTH_MAX=20
RATE_LIMIT_AUTH_WINDOW_SECONDS=60
RELAY_EXPOSE_UPSTREAM_ERRORS=false
RELAY_OBSERVABILITY_CAPTURE_BODY=false
